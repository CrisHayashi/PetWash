<div id="produtoMenu" class="container">
    <div class="row">
        <div class="col s3">
            <a id="btn_novoProduto" class="waves-effect waves-light btn">Novo</a>
        </div>

        <form id="produtoForm" class="col s12">
            <div class="row">
                <div class="input-field col s6">
                    <i class="material-icons prefix">person</i>
                    <input id="nameProduto" name="name" type="text" data-label="Nome do Produto" class="validate">
                    <label for="nameProduto">Nome do Produto</label>
                </div>
                <div class="input-field col s6">
                    <i class="material-icons prefix">attach_money</i>
                    <input id="price" name="price" type="number" data-label="Preço" class="validate">
                    <label for="price">Preço</label>
                </div>
                <div class="input-field col s6">
                    <i class="material-icons prefix">phone</i>
                    <input id="category" name="category" type="text" data-label="Categoria" class="validate">
                    <label for="category">Categoria</label>
                </div>
                <div class="input-field col s6">
                    <i class="material-icons prefix">phone</i>
                    <input id="stock" name="stock" type="number" data-label="Estoque" class="validate">
                    <label for="stock">Estoque</label>
                </div>
                <div class="input-field col s12">
                    <!-- está criando uma div invisível para oculpar o bloco -->
                </div>
                <div class="col s3">
                    <a id="btn_salvarProduto" class="waves-effect waves-light btn">Enviar</a>
                </div>
                <div class="col s3">
                    <a id="btn_voltarProduto" class="waves-effect waves-light btn">Voltar</a>
                </div>
            </div>
        </form>
    </div>

    <table id="table_produto">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Preço</th>
                <th>Categoria</th>
                <th>Estoque</th>
                <th>Ações</th>
            </tr>
        </thead>

        <tbody id="produto_list">
            <!-- lista de productes gerada aqui -->
             <tbody id="product_list">
                    <% if (products && products.length> 0) { %>
                        <% products.forEach(product=> { %>
                            <tr>
                                <td>
                                    <%= product.name %>
                                </td>
                                <td>
                                    <%= product.price %>
                                </td>
                                <td>
                                    <%= product.category %>
                                </td>
                                <td>
                                    <%= product.stock %>
                                </td>
                                <td>
                                    <!-- botões de ação (editar, excluir, etc) -->
                                    <button class="btn-small blue">Editar</button>
                                    <button class="btn-small red">Excluir</button>
                                </td>
                            </tr>
                            <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="6">Nenhum product cadastrado.</td>
                                    </tr>
                                    <% } %>
                </tbody>
        </tbody>
    </table>

</div>